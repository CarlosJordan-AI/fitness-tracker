import httpx
import asyncio
import json
import os

async def main():
    # Login to pocketbase as admin or user to get a token, or just try without token
    # Wait, we know we can't save without token?
    pass

if __name__ == '__main__':
    pass
